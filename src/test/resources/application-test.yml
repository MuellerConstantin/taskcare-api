spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
taskcare:
  security:
    jwt:
      secret: 0xCAFEBABE
      expiresIn: 600000
